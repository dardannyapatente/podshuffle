<div class="container">
        <div class="row">
<h1>MY FAVORITE PODCASTS</h1>

{{#each favorites}}
<div class="podcast-content">
<ul>
    <li>
      <img id="favorite" class="rounded mx-auto d-block" src="{{favoritePodcastImage}}" alt="{{favoritePodcastTitle}}" />
      <a href="/episode/podcast/{{favoritePodcastId}}">
            <h4>{{favoritePodcastTitle}}</h4>
      </a>
    </li>
</ul>

<form action="/favorite/{{isFavorited._id}}/delete" method="POST">
  <button id="heart-btn">♥️</button>
</form>
{{/each}}
</div>
</div>